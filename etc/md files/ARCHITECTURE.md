# 📐 Kannada Learning App - Architecture & Flow

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                         USER BROWSER                         │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │              Frontend (HTML/CSS/JS)                     │ │
│  │                                                         │ │
│  │  ┌──────────────┐         ┌──────────────┐            │ │
│  │  │   Student    │         │    Admin     │            │ │
│  │  │   Interface  │         │    Panel     │            │ │
│  │  │              │         │              │            │ │
│  │  │ • Chapters   │         │ • Add Ch.    │            │ │
│  │  │ • Study      │         │ • Edit Ch.   │            │ │
│  │  │ • Quiz       │         │ • Add Quiz   │            │ │
│  │  │ • Results    │         │ • Manage     │            │ │
│  │  └──────────────┘         └──────────────┘            │ │
│  │         │                         │                    │ │
│  │         └────────────┬────────────┘                    │ │
│  │                      │                                 │ │
│  │              ┌───────▼────────┐                        │ │
│  │              │   API Service   │                       │ │
│  │              │   (api.js)      │                       │ │
│  │              └───────┬─────────┘                       │ │
│  └──────────────────────┼──────────────────────────────┘ │
│                          │                                 │
│                          │ HTTP Requests                   │
│                          │ (REST API)                      │
└──────────────────────────┼─────────────────────────────────┘
                           │
                  ┌────────▼────────┐
                  │   CORS Headers   │
                  └────────┬─────────┘
                           │
┌──────────────────────────▼─────────────────────────────────┐
│              Backend Server (Node.js + Express)            │
│                    Port: 3000                              │
│                                                            │
│  ┌──────────────────────────────────────────────────────┐ │
│  │                  API Routes                           │ │
│  │                                                       │ │
│  │  • GET  /api/chapters         → List all            │ │
│  │  • POST /api/chapters         → Create new          │ │
│  │  • PUT  /api/chapters/:id     → Update              │ │
│  │  • DELETE /api/chapters/:id   → Delete              │ │
│  │                                                       │ │
│  │  • GET  /api/quizzes/chapter/:id → Get quiz        │ │
│  │  • POST /api/quizzes             → Save quiz       │ │
│  │  • POST /api/quizzes/:id/submit  → Calculate score │ │
│  └──────────────────────────────────────────────────────┘ │
│                           │                                │
│  ┌────────────────────────▼──────────────────────────────┐│
│  │              File System Operations                    ││
│  │                                                        ││
│  │  • fs.readFile()  - Read JSON                         ││
│  │  • fs.writeFile() - Write JSON                        ││
│  │  • JSON.parse()   - Deserialize                       ││
│  │  • JSON.stringify() - Serialize                       ││
│  └───────────────────────────────────────────────────────┘│
│                           │                                │
└───────────────────────────┼────────────────────────────────┘
                            │
            ┌───────────────▼───────────────┐
            │   Data Storage (JSON Files)    │
            │                                │
            │  ┌──────────────────────────┐ │
            │  │   chapters.json          │ │
            │  │   [                      │ │
            │  │     {                    │ │
            │  │       id: 1,             │ │
            │  │       chapterNumber: 1,  │ │
            │  │       title: "...",      │ │
            │  │       studyMaterial: "" │ │
            │  │       summary: "..."     │ │
            │  │     }                    │ │
            │  │   ]                      │ │
            │  └──────────────────────────┘ │
            │                                │
            │  ┌──────────────────────────┐ │
            │  │   quizzes.json           │ │
            │  │   [                      │ │
            │  │     {                    │ │
            │  │       id: 1,             │ │
            │  │       chapterId: 1,      │ │
            │  │       questions: [...]   │ │
            │  │     }                    │ │
            │  │   ]                      │ │
            │  └──────────────────────────┘ │
            └────────────────────────────────┘
```

## 🔄 User Flow Diagrams

### Admin Flow - Adding a Chapter

```
┌─────────────┐
│   Admin     │
│   Clicks    │
│  "Admin"    │
└──────┬──────┘
       │
       ▼
┌─────────────────┐
│   Fill Form     │
│ • Chapter #     │
│ • Title         │
│ • Study Text    │
│ • Summary       │
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│  Click "Save"   │
└──────┬──────────┘
       │
       ▼
┌─────────────────────┐
│ POST /api/chapters  │
│   {data...}         │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│ Backend validates   │
│ Assigns ID          │
│ Writes to JSON      │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  Returns success    │
│  Shows in list      │
└─────────────────────┘
```

### Admin Flow - Creating a Quiz

```
┌─────────────┐
│   Select    │
│  Chapter    │
└──────┬──────┘
       │
       ▼
┌─────────────────┐
│ Click "+ Add    │
│   Question"     │
└──────┬──────────┘
       │
       ▼
┌─────────────────────┐
│  Fill Question      │
│ • Question text     │
│ • 4 Options (ABCD)  │
│ • Correct answer    │
│ • Explanation       │
└──────┬──────────────┘
       │
       ▼
┌─────────────────┐
│  Repeat for     │
│  more questions │
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│  Click "Save    │
│     Quiz"       │
└──────┬──────────┘
       │
       ▼
┌─────────────────────┐
│ POST /api/quizzes   │
│ {chapterId, qs[]}   │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│ Backend saves to    │
│  quizzes.json       │
└─────────────────────┘
```

### Student Flow - Taking a Quiz

```
┌─────────────┐
│   Browse    │
│  Chapters   │
└──────┬──────┘
       │
       ▼
┌─────────────────┐
│  Click Chapter  │
└──────┬──────────┘
       │
       ▼
┌──────────────────────┐
│ Modal Opens          │
│ • Study Material     │
│ • Summary            │
│ • "Start Quiz" btn   │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐
│  Click "Start Quiz"  │
└──────┬───────────────┘
       │
       ▼
┌─────────────────────────┐
│ GET /api/quizzes/       │
│      chapter/:id        │
└──────┬──────────────────┘
       │
       ▼
┌─────────────────────┐
│  Quiz Loads         │
│  Show all questions │
│  with MCQ options   │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  Student selects    │
│  answers (A/B/C/D)  │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  Click "Submit"     │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────────┐
│  Validation:            │
│  All answered?          │
└──────┬──────────────────┘
       │ Yes
       ▼
┌─────────────────────────┐
│ POST /api/quizzes/      │
│      :id/submit         │
│ {answers: ['A','B'...]} │
└──────┬──────────────────┘
       │
       ▼
┌─────────────────────────┐
│  Backend calculates:    │
│  • Correct count        │
│  • Percentage           │
│  • Per-question results │
└──────┬──────────────────┘
       │
       ▼
┌─────────────────────────┐
│  Results Modal          │
│  • Score: 80%           │
│  • 8/10 correct         │
│  • Question breakdown   │
│  • Correct answers      │
│  • Explanations         │
└─────────────────────────┘
       │
       ▼
┌─────────────────────────┐
│  Options:               │
│  • Return to chapter    │
│  • Close                │
└─────────────────────────┘
```

## 📊 Data Flow

### Chapter Creation Data Flow

```
Admin Form Input
      ↓
JavaScript captures form data
      ↓
{
  chapterNumber: 1,
  title: "ಜೀವರಹುಳಿ",
  studyMaterial: "...",
  summary: "..."
}
      ↓
api.js → POST to /api/chapters
      ↓
Express route handler
      ↓
Read chapters.json
      ↓
Add new ID
      ↓
Push to array
      ↓
Write back to chapters.json
      ↓
Return new chapter object
      ↓
Frontend updates UI
      ↓
Shows success message
```

### Quiz Submission Data Flow

```
Student answers quiz
      ↓
Answers array: ['A', 'C', 'B', 'D', ...]
      ↓
api.js → POST to /api/quizzes/:id/submit
      ↓
Backend fetches quiz from quizzes.json
      ↓
Loop through questions:
  For each question:
    - Compare user answer vs correct answer
    - Count if correct
    - Build result object
      ↓
Calculate:
  - correctCount / totalQuestions
  - percentage = (correct/total) * 100
      ↓
Return results object:
{
  totalQuestions: 10,
  correctAnswers: 8,
  percentage: 80.00,
  results: [
    {
      question: "...",
      userAnswer: "A",
      correctAnswer: "A",
      isCorrect: true
    },
    ...
  ]
}
      ↓
Frontend displays results
```

## 🎨 Frontend Module Breakdown

```
frontend/
│
├── index.html
│   ├── Structure & Layout
│   ├── Navigation Bar
│   ├── Student View Container
│   ├── Admin View Container
│   └── Modal Containers
│
├── css/styles.css
│   ├── Global Styles
│   ├── Layout (Flexbox/Grid)
│   ├── Components (Cards, Buttons, Forms)
│   ├── Modals
│   ├── Responsive Breakpoints
│   └── Kannada Font Import
│
└── js/
    ├── api.js
    │   └── API wrapper functions
    │       ├── chapters CRUD
    │       └── quizzes CRUD
    │
    ├── student.js
    │   ├── loadChapters()
    │   ├── openChapter()
    │   ├── startQuiz()
    │   ├── submitQuiz()
    │   └── showResults()
    │
    ├── admin.js
    │   ├── loadAdminChapters()
    │   ├── Chapter form handler
    │   ├── editChapter()
    │   ├── deleteChapter()
    │   ├── Quiz form handler
    │   ├── addQuestion()
    │   └── removeQuestion()
    │
    └── app.js
        ├── Initialize app
        ├── showView() - Switch views
        ├── showAdminTab() - Switch tabs
        └── Backend health check
```

## 🔐 Security Considerations

```
Current Setup (Development):
┌────────────────────────────┐
│  No Authentication         │
│  Anyone can be admin       │
│  Local network only        │
│  No data encryption        │
└────────────────────────────┘

For Production:
┌────────────────────────────┐
│  Add User Login            │
│  Role-based access         │
│  JWT tokens                │
│  HTTPS                     │
│  Input sanitization        │
│  Rate limiting             │
└────────────────────────────┘
```

## ⚡ Performance Optimization

```
Current:
• JSON files (fast for small data)
• No database overhead
• Direct file I/O
• In-memory operations
• < 100ms response times

Scalability:
• Good for 10 users
• Good for 10 chapters
• Good for ~100 questions total
• Instant page loads
• No caching needed (data is small)

If scaling up:
• Switch to database (PostgreSQL)
• Add Redis caching
• Implement pagination
• Compress responses
• Use CDN for static files
```

---

This architecture is optimized for:
✅ Simplicity
✅ Fast development
✅ Easy maintenance
✅ Small user base (10 users)
✅ Quick deployment
✅ No external dependencies
